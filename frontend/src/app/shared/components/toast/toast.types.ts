export type ToastType = 'success' | 'error' | 'warning' | 'info';

export interface Toast {
  id: string;
  message: string;
  type: ToastType;
  duration: number;
  leaving: boolean;
}

export interface ToastOptions {
  duration?: number;
}